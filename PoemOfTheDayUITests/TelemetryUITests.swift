import XCTest

final class TelemetryUITests: XCTestCase {
    func testTelemetryCoveredByIntegration() throws {
        throw XCTSkip("Telemetry verification is covered by integration tests with mocks, not UI tests.")
    }
}
