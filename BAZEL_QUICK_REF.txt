╔═══════════════════════════════════════════════════════════════════╗
║              BAZEL QUICK REFERENCE - Poem of the Day              ║
╚═══════════════════════════════════════════════════════════════════╝

┌───────────────────────────────────────────────────────────────────┐
│ 🚀 QUICK START                                                    │
└───────────────────────────────────────────────────────────────────┘

  # 1. Install Bazelisk
  brew install bazelisk

  # 2. Make script executable  
  chmod +x bazel.sh

  # 3. Build the app
  ./bazel.sh build

  # 4. Run tests
  ./bazel.sh test

  # 5. Full CI check
  ./bazel.sh ci


┌───────────────────────────────────────────────────────────────────┐
│ 🛠️  COMMON COMMANDS                                               │
└───────────────────────────────────────────────────────────────────┘

  Command                    Description
  ─────────────────────────────────────────────────────────────────
  ./bazel.sh build           Build the iOS app (debug)
  ./bazel.sh build --release Build release version
  ./bazel.sh test            Run all tests
  ./bazel.sh unit-test       Run unit tests only
  ./bazel.sh ui-test         Run UI tests only
  ./bazel.sh coverage        Generate coverage report
  ./bazel.sh clean           Clean build cache
  ./bazel.sh deep-clean      Nuclear clean (removes everything)
  ./bazel.sh ci              Run full CI pipeline locally
  ./bazel.sh query           Show build dependencies
  ./bazel.sh graph           Generate dependency graph (needs graphviz)
  ./bazel.sh help            Show all commands

  ALTERNATIVE: Use 'make' instead
  ─────────────────────────────────────────────────────────────────
  make build                 Same as ./bazel.sh build
  make test                  Same as ./bazel.sh test
  make ci                    Same as ./bazel.sh ci
  make help                  Show Makefile commands


┌───────────────────────────────────────────────────────────────────┐
│ 📁 KEY FILES                                                      │
└───────────────────────────────────────────────────────────────────┘

  File                       Purpose
  ─────────────────────────────────────────────────────────────────
  WORKSPACE                  External dependencies configuration
  BUILD                      Build targets (app, tests, libs)
  .bazelrc                   Build flags and compiler options
  .bazelversion              Pins Bazel version (7.0.0)
  .bazelignore               Files for Bazel to ignore
  bazel.sh                   Helper script (recommended)
  Makefile                   Alternative interface
  .github/workflows/         CI/CD pipeline configuration
    bazel-ci.yml


┌───────────────────────────────────────────────────────────────────┐
│ 📚 DOCUMENTATION                                                  │
└───────────────────────────────────────────────────────────────────┘

  Document                   What's Inside
  ─────────────────────────────────────────────────────────────────
  BAZEL_CHECKLIST.md         Step-by-step setup checklist
  BAZEL_BUILD.md             Complete usage guide
  BAZEL_MIGRATION.md         Migration instructions
  BAZEL_ARCHITECTURE.md      System architecture diagrams
  BAZEL_SETUP_SUMMARY.md     What was done + next steps
  README.md                  Updated with Bazel instructions


┌───────────────────────────────────────────────────────────────────┐
│ 🐛 TROUBLESHOOTING                                                │
└───────────────────────────────────────────────────────────────────┘

  Problem                    Solution
  ─────────────────────────────────────────────────────────────────
  Build fails                bazelisk build --verbose_failures //...
  Can't find Xcode           sudo xcode-select -s /Applications/Xcode.app
  Simulator not found        xcrun simctl list devices
                            Update .bazelrc with valid device
  Tests fail                 Check .bazelrc simulator settings
  Cache issues               ./bazel.sh deep-clean
  CI fails                   Check Xcode version in workflow file


┌───────────────────────────────────────────────────────────────────┐
│ 🎯 BUILD TARGETS                                                  │
└───────────────────────────────────────────────────────────────────┘

  Target                     Description
  ─────────────────────────────────────────────────────────────────
  //:PoemOfTheDay            iOS application bundle
  //:PoemOfTheDayLib         Main Swift library
  //:PoemOfTheDayTests       Unit test suite
  //:PoemOfTheDayUITests     UI test suite

  # Build specific target
  bazelisk build //:PoemOfTheDay

  # Test specific target
  bazelisk test //:PoemOfTheDayTests


┌───────────────────────────────────────────────────────────────────┐
│ ⚙️  CONFIGURATIONS                                                │
└───────────────────────────────────────────────────────────────────┘

  Config                     Usage
  ─────────────────────────────────────────────────────────────────
  --config=debug             Debug build (default)
  --config=release           Release/optimized build
  --config=ci                CI-specific settings

  # Example
  bazelisk build --config=release //:PoemOfTheDay


┌───────────────────────────────────────────────────────────────────┐
│ 🧪 TESTING                                                        │
└───────────────────────────────────────────────────────────────────┘

  Command                              Description
  ─────────────────────────────────────────────────────────────────
  bazelisk test //...                  Run all tests
  bazelisk test --test_output=all //...    Show all output
  bazelisk test --test_filter=VibeAnalyzer*  Run specific test
  bazelisk test --ios_simulator_device=     Use specific simulator
    "iPhone 15" //...

  # Coverage
  bazelisk coverage //:PoemOfTheDayTests
  # Report: bazel-out/_coverage/_coverage_report.dat


┌───────────────────────────────────────────────────────────────────┐
│ 📊 CI/CD                                                          │
└───────────────────────────────────────────────────────────────────┘

  The GitHub Actions workflow runs on every push/PR:

  ✓ Build app with Bazel
  ✓ Run unit tests
  ✓ Run UI tests  
  ✓ Generate coverage
  ✓ Lint with SwiftLint
  ✓ Check formatting
  ✓ Analyze dependencies
  ✓ Run performance tests

  # Simulate CI locally
  ./bazel.sh ci


┌───────────────────────────────────────────────────────────────────┐
│ 🔍 QUERY COMMANDS                                                 │
└───────────────────────────────────────────────────────────────────┘

  # Show all build targets
  bazelisk query //...

  # Show dependencies
  bazelisk query 'deps(//:PoemOfTheDay)'

  # Find Swift files
  bazelisk query 'kind("swift_library", //...)'

  # Check for circular deps
  bazelisk query 'allpaths(//:PoemOfTheDayLib, //:PoemOfTheDayLib)'


┌───────────────────────────────────────────────────────────────────┐
│ 🚀 PERFORMANCE                                                    │
└───────────────────────────────────────────────────────────────────┘

  # Profile build
  bazelisk build --profile=profile.json //:PoemOfTheDay
  bazelisk analyze-profile profile.json

  # Parallel builds (auto-detect cores)
  bazelisk build --jobs=auto //:PoemOfTheDay

  # Show build explanation
  bazelisk build --explain=explain.txt //:PoemOfTheDay


┌───────────────────────────────────────────────────────────────────┐
│ 💡 PRO TIPS                                                       │
└───────────────────────────────────────────────────────────────────┘

  1. Cache is key
     • Local cache: ~/.bazel_cache
     • CI cache: GitHub Actions caching
     • Remote cache: Configure in .bazelrc

  2. Use helper script
     • ./bazel.sh is easier than raw bazelisk commands
     • Or use 'make' for even shorter commands

  3. Run CI locally
     • ./bazel.sh ci catches issues before push
     • Saves time and CI minutes

  4. Both Xcode and Bazel work
     • Keep using Xcode for development
     • Use Bazel for CI/CD
     • They complement each other!

  5. Incremental builds are fast
     • First build: ~3-5 minutes
     • Incremental: ~10-30 seconds
     • Cache makes it even faster


┌───────────────────────────────────────────────────────────────────┐
│ 🆘 GETTING HELP                                                   │
└───────────────────────────────────────────────────────────────────┘

  Resource                   Location
  ─────────────────────────────────────────────────────────────────
  Command help               ./bazel.sh help
  Build guide                BAZEL_BUILD.md
  Migration guide            BAZEL_MIGRATION.md
  Setup checklist            BAZEL_CHECKLIST.md
  Architecture               BAZEL_ARCHITECTURE.md
  Bazel docs                 https://bazel.build/
  Rules Apple                github.com/bazelbuild/rules_apple


┌───────────────────────────────────────────────────────────────────┐
│ ✅ VERIFICATION                                                   │
└───────────────────────────────────────────────────────────────────┘

  Run these to verify setup:

  □ bazelisk version             # Should show 7.0.0
  □ ./bazel.sh build             # Should complete successfully
  □ ./bazel.sh test              # All tests should pass
  □ ./bazel.sh ci                # Full CI should succeed
  □ git push                     # CI should be green on GitHub


┌───────────────────────────────────────────────────────────────────┐
│ 📞 SUPPORT                                                        │
└───────────────────────────────────────────────────────────────────┘

  • Check documentation files first
  • Create GitHub issue for bugs
  • Use GitHub Discussions for questions
  • See BAZEL_CHECKLIST.md for common issues


╔═══════════════════════════════════════════════════════════════════╗
║  TIP: Start with './bazel.sh build' - if it fails, the error     ║
║       message will tell you exactly what needs fixing!            ║
╚═══════════════════════════════════════════════════════════════════╝

Version: 1.0 | Bazel: 7.0.0 | iOS: 18.0+ | Xcode: 16.0+
